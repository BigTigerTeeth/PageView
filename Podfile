# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'PageView' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PageView
pod 'SnapKit', '~> 0.22'
  target 'PageViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PageViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
